/**
 * GetFunction handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: functions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getFunction } from "@ceilgas/api-server/domains/functions/handlers";
import type { FunctionsDDDDependencies } from "@ceilgas/api-server/domains/functions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ceilgas/tests/shared";
import { FUNCTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetFunction", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: FunctionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<FunctionsDDDDependencies>(
      FUNCTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetFunction, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { functionId: entity.id };

    await getFunction(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetFunction.execute).toHaveBeenCalledWith(expect.objectContaining({ functionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { functionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetFunction, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getFunction(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
